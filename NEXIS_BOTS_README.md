# 🤖 Nexis - Система ботов для NovaChat

## Описание

**Nexis** — это система ботов для мессенджера NovaChat, вдохновленная ботами Telegram. Боты позволяют автоматизировать задачи, предоставлять информацию и создавать интерактивные сценарии в чатах.

## Что такое Nexis бот?

Бот — это программа (а не человек), которая:
- 🤖 Не является настоящим пользователем
- 💬 Реагирует на сообщения и команды в чатах
- 📱 Работает с кнопками и интерактивными элементами
- 🔄 Может автоматизировать рутинные задачи
- ❌ **Не может** писать первым в чат
- ❌ **Не может** читать личные чаты
- ❌ **Не может** видеть номер телефона

## Возможности Nexis ботов

### 1️⃣ Работа с командами

Боты реагируют на команды, начинающиеся с `/`:

```
/start      - Начать взаимодействие с ботом
/help       - Получить справку по командам
/settings   - Открыть настройки бота
```

Каждый бот может иметь свои собственные команды.

### 2️⃣ Интерактивные кнопки

Боты могут отправлять сообщения с кнопками:
- Встроенные кнопки (inline buttons)
- Реакция на клики
- Интерактивные меню

### 3️⃣ Автоматизация

Боты могут:
- ✅ Отправлять уведомления
- ✅ Искать информацию
- ✅ Управлять задачами
- ✅ Обрабатывать команды
- ✅ Логировать действия пользователей

### 4️⃣ Логирование

Все действия пользователей с ботом записываются в логи для анализа.

## API Endpoints

### Получить все боты
```bash
GET /api/bots
```
**Ответ:**
```json
{
  "ok": true,
  "bots": [
    {
      "id": 1,
      "name": "NewsBot",
      "description": "Новости и обновления",
      "avatar_url": "https://...",
      "is_active": true,
      "subscriber_count": 150,
      "created_at": "2026-02-02T10:00:00Z"
    }
  ]
}
```

### Получить информацию о боте
```bash
GET /api/bots/:botId
```

### Создать нового бота
```bash
POST /api/bots
Content-Type: application/json

{
  "name": "MyBot",
  "description": "Описание бота",
  "avatar_url": "https://...",
  "commands": [
    {
      "cmd": "/start",
      "description": "Начать"
    }
  ]
}
```

### Подписаться на бота
```bash
POST /api/bots/:botId/subscribe
```

### Отписаться от бота
```bash
POST /api/bots/:botId/unsubscribe
```

### Получить подписки пользователя
```bash
GET /api/user/bot-subscriptions
```

### Отправить команду боту
```bash
POST /api/bots/:botId/command
Content-Type: application/json

{
  "command": "/start",
  "chatId": 5,
  "params": {}
}
```

### Обработать клик по кнопке
```bash
POST /api/bots/:botId/button-click
Content-Type: application/json

{
  "action": "help",
  "chatId": 5
}
```

## Структура базы данных

### Таблица `nexis_bots`
Хранит информацию о ботах:
- `id` - ID бота
- `name` - Имя бота (уникальное)
- `description` - Описание
- `avatar_url` - URL аватара
- `creator_id` - ID создателя
- `is_active` - Активен ли бот
- `commands` - JSON массив команд
- `created_at` - Дата создания
- `updated_at` - Дата обновления

### Таблица `nexis_bot_subscribers`
Подписки пользователей на ботов:
- `id` - ID записи
- `bot_id` - ID бота
- `user_id` - ID пользователя
- `subscribed_at` - Дата подписки

### Таблица `nexis_bot_messages`
Сообщения, отправленные ботами:
- `id` - ID сообщения
- `bot_id` - ID бота
- `chat_id` - ID чата
- `message_text` - Текст сообщения
- `buttons` - JSON массив с кнопками
- `created_at` - Дата отправки

### Таблица `nexis_bot_actions`
Логирование действий пользователей:
- `id` - ID записи
- `bot_id` - ID бота
- `user_id` - ID пользователя
- `action_type` - Тип действия (message, command, button_click)
- `action_data` - JSON данные действия
- `created_at` - Дата действия

## Примеры использования

### Пример 1: Простой бот с командами

```javascript
// На фронтенде
const createBotResponse = await fetch('/api/bots', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    name: 'WeatherBot',
    description: 'Получать информацию о погоде',
    commands: [
      { cmd: '/weather', description: 'Текущая погода' },
      { cmd: '/forecast', description: 'Прогноз' }
    ]
  })
});
```

### Пример 2: Подписка на бота

```javascript
await fetch('/api/bots/1/subscribe', {
  method: 'POST'
});
```

### Пример 3: Отправка команды

```javascript
const response = await fetch('/api/bots/1/command', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    command: '/start',
    chatId: 5
  })
});
```

## Ограничения

❌ **Боты НЕ могут:**
- Писать первыми в чат
- Читать личные сообщения между пользователями
- Видеть номер телефона пользователя
- Маскироваться под человека

✅ **Боты МОГУТ:**
- Реагировать на сообщения в групповых чатах
- Обрабатывать команды
- Отправлять кнопки и интерактивные элементы
- Автоматизировать задачи
- Логировать действия

## События Socket.IO

### Сообщение от бота
```javascript
socket.on('message-from-bot', (data) => {
  console.log(data);
  // {
  //   botId: 1,
  //   botName: 'MyBot',
  //   botAvatar: 'https://...',
  //   text: 'Привет!',
  //   buttons: [{ text: 'Кнопка', action: 'callback' }],
  //   timestamp: '2026-02-02T10:00:00Z'
  // }
});
```

### Создание бота
```javascript
socket.on('bot-created', (bot) => {
  console.log('Новый бот создан:', bot);
});
```

## Безопасность

1. **Авторизация** - только авторизованные пользователи могут:
   - Подписываться на ботов
   - Создавать ботов
   - Отправлять команды

2. **Ограничения команд** - боты не могут выполнять опасные операции

3. **Логирование** - все действия логируются для аудита

4. **Валидация** - все входные данные проверяются и очищаются

## Примеры ботов

### NewsBot 📰
Получение последних новостей и обновлений

### WeatherBot 🌤️
Информация о погоде

### ReminderBot ⏰
Напоминания и уведомления

### TaskBot ✅
Управление задачами и TODO

### TranslatorBot 🌐
Переводы текстов

## Развертывание

Боты работают на сервере Node.js с использованием:
- Express.js - REST API
- PostgreSQL - база данных
- Socket.IO - real-time коммуникация

## Поддержка

Для вопросов и предложений по развитию Nexis обратитесь к администратору.

---

**Версия:** 1.0.0  
**Дата обновления:** 2 февраля 2026 г.
